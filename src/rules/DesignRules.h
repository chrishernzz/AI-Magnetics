#pragma once

// KEPT — engineering heuristics layer (Option 1 from the original
// three-option framing), unchanged in role: still sits on top of the
// physics engine to flag "technically valid but a real engineer
// wouldn't do this" designs. Content will need revisiting for
// buck-specific heuristics (e.g. gap fringing rules of thumb) later.
