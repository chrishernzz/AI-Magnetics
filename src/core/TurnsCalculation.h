// #pragma once

// #include "CoreSelection.h"
// #include "GapDesign.h"

// // STAGE 5: Turns
// //
// // REPLACES TurnsAndInductance.cpp from the flyback version.
// // Difference: no turns RATIO here — a buck inductor is a single
// // winding, not primary/secondary, since there's no isolation barrier.
// // Also, L is now an input (given), not something this stage derives.

// int calculateTurns(const CoreParams& core, const GapResult& gap, double inductanceH, double peakCurrentA);
