[project]
name = "aimagnetics"
version = "0.1.0"
description = "AI Magnetics"
requires-python = ">=3.11"

dependencies = [
    "fastapi",
    "uvicorn[standard]"
]

[tool.pytest.ini_options]
testpaths = ["tests/python"]